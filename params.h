#define DRANK (3) // number of data dimensions
#define DWIDTH (480) // number of data elements in each dimension
#define TILES (2) // number of process tiles in each dimension
#define TWIDTH (DWIDTH/TILES)

#define SLICEAXIS (2) // which axis to take a slice along
